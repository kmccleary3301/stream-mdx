# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) to manage multi-package versioning and changelogs.

Common commands:

- `npm run changeset` — create a changeset
- `npm run changeset:version` — apply version bumps + changelog updates
- `npm run changeset:publish` — publish packages (when ready)



