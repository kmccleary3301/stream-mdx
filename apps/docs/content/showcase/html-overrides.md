# HTML Overrides (ShadCN)

This page is a placeholder for a richer feature showcase article.

- Goal: show that HTML elements can be mapped onto ShadCN components without compromising streaming performance.
- Examples to add:
  - `<table>` → ShadCN table components
  - `<blockquote>` → styled blockquote
  - overflow-safe wrappers for code/math/tables

