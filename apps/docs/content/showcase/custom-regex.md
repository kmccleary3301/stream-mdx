# Custom Regex Plugins

This page is a placeholder for a richer feature showcase article.

- Goal: show domain-specific transforms (e.g. `@mentions`, `@cite{...}`, custom tokens) that work during streaming.

