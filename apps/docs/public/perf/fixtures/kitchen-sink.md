# Kitchen Sink

A paragraph with *emphasis*, **strong**, `inline code`, and a link to [example](https://example.com).

> A blockquote with `inline code` and **strong** text.

## Lists

1. First ordered item
2. Second ordered item with *emphasis*
   - Nested bullet
   - Another bullet
3. Third item

## Table

| Name | Value | Notes |
| --- | ---: | --- |
| Alpha | 10 | Inline `code` |
| Beta | 20 | **Bold** text |
| Gamma | 30 | A link: [docs](https://example.com/docs) |

## Code Block

```ts
const hello = "world";
console.log(hello);
```

## Inline HTML

Use <kbd>Ctrl</kbd> + <kbd>K</kbd> to search. H2O is H<sub>2</sub>O and E = mc<sup>2</sup>.

## Math

Inline math: $E = mc^2$ and $f(x) = x^2$.

Display math:

$$
\mathbf{J}_f(\mathbf{x}) = \nabla f(\mathbf{x})
$$

## Footnotes

A statement with a footnote.[^note]

[^note]: This is the footnote text.
