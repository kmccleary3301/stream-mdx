# Inline `Code` In Headings

This fixture exists to ensure heading text extraction and slugging are deterministic across:
- Node `worker_threads` snapshot compile
- Browser WebWorker compile

## Heading With `inline` code

### Heading With Emphasis *and* `code`

#### `Code` Only

##### Mixed `code` and punctuation: hello, world!

Paragraph after.

