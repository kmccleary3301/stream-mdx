# MDX Expression No Swallow Negative

Prefix text with {expr and trailing prose that must stay visible.

- List item with {nestedExpr and sibling prose that must remain visible.
  - Nested item with {nestedChildExpr and sibling prose that must remain visible.

> Blockquote with {quotedExpr and trailing content.
>
> - Quoted list item with {quotedNestedExpr and quoted sibling prose that must remain visible.

Final paragraph after the MDX expression negatives.
