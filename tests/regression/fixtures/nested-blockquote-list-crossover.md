- Root bullet with partial emphasis *alpha
  > Blockquote layer with partial code `beta
  > - Nested list in quote with cite {cite:12
  > - Another nested item with inline math $x + y
  - Return to sibling list item with ++plugin-like marker
