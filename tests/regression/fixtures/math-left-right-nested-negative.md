# Math V2A Left/Right Nested Negative

This case should remain conservative:

$$
\left( \frac{\left[a+b}{c}
$$

Expected direction:
- detect nested left/right pressure
- prefer hard-stop / raw fallback over speculative balancing
