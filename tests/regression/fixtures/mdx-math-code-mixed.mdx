# MDX Math Code Mixed

This fixture mixes inline math like $E = mc^2$, standalone code blocks, and multiple finalized MDX preview blocks.

<Preview codeblock>
Stability note:

1. First ordered item with `inline code`
2. Second ordered item with math $a^2 + b^2 = c^2$
</Preview>

```text
Stability note:

1. First ordered item with `inline code`
2. Second ordered item with math $a^2 + b^2 = c^2$
```

```ts
export const answer = 42;
```

<Preview>
| Model | Score | Notes |
| --- | --- | --- |
| Alpha | 0.91 | stable |
| Beta | 0.87 | review |
</Preview>

$$
\int_0^1 x^2 \, dx = \frac{1}{3}
$$

```python
def square(x: int) -> int:
    return x * x
```
