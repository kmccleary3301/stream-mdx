# Nested List Code Blocks

1. Ordered item with nested TypeScript:

   ```ts
   const value = 1;
   console.log(value);
   ```

2. Ordered item with nested bullets:
   - Bullet child with nested shell snippet:

     ```bash
     echo "nested"
     printf '%s\n' done
     ```

- Unordered item with nested JavaScript:

  ```js
  export const answer = 42;
  console.log(answer);
  ```
