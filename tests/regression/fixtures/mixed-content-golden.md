# Mixed Content Golden

This fixture mixes *emphasis*, **strong**, `inline code`, and a link to [example](https://example.com).

## Inline HTML + Math

Use <kbd>Ctrl</kbd> + <kbd>K</kbd> to search. Water is H<sub>2</sub>O and energy is E = mc<sup>2</sup>.
Inline math: $E = mc^2$ and $f(x) = x^2$.

Display math:

$$
\mathbf{J}_f(\mathbf{x}) = \nabla f(\mathbf{x})
$$

## Table

| Name | Value | Notes |
| --- | ---: | --- |
| Alpha | 10 | Inline `code` |
| Beta | 20 | **Bold** text |
| Gamma | 30 | A link: [docs](https://example.com/docs) |

## Blockquote

> A quote with `inline code` and **strong** text.

## Lists

1. First ordered item
2. Second ordered item with *emphasis*
   - Nested bullet
   - Another bullet
3. Third item

## Code Block

```ts
const hello = "world";
console.log(hello);
```

## Footnotes

A statement with a footnote.[^note]

[^note]: This is the footnote text.
